# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# prisma
/prisma/*.db
/prisma/*.db-journal

# env
.env
.env*.local

# uploaded files (dev). In production these live on Docker volumes, not in the
# source tree — see docker-compose.yml. private-uploads holds the gated brand
# assets, which is doubly a reason not to commit one by accident.
/uploads
/private-uploads

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/*
!.vscode/extensions.json
.idea
